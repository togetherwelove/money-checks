import { ICON_ACTION_BUTTON_SIZE } from "../components/IconActionButton";

export const DateNavigationToolbarLayout = {
  compactBottomPadding: 2,
  defaultBottomPadding: 8,
  defaultMinHeight: ICON_ACTION_BUTTON_SIZE,
  defaultTopPadding: 8,
  indicatorSize: 4,
  indicatorTopMargin: 1,
  labelGap: 6,
} as const;
